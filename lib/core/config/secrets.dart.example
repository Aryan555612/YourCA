// Copy this file to secrets.dart and fill in your own keys.
// secrets.dart is gitignored — never commit the real keys.
class AppSecrets {
  /// Brevo transactional email API key.
  /// Get yours free at https://app.brevo.com/settings/keys/api
  static const String brevoApiKey = 'YOUR_BREVO_API_KEY_HERE';

  /// Verified sender email address in your Brevo account.
  static const String brevoSenderEmail = 'your-email@gmail.com';
}
